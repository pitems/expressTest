<h1 class="text-center mb-3">{{title}}</h1>
<h4>Members</h4>
<ul class="list-group">
    {{#each members}}
    <li class="list-group-item">{{this.name}}:{{this.email}}</li>
    {{/each}}
</ul> 

<a href="/about" class="btn btn-light mt-4">About</a>
<a href="/form" class="btn btn-light mt-4">Form</a>